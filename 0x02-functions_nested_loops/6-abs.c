#include "main.h"

/**
 * _abs - function that computes an absolute value of an integer
 * @n: absolute value of integer
 * Return: the absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
